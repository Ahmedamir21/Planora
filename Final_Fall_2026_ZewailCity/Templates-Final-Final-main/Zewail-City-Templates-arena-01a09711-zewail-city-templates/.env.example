# Vercel server-side environment variables
# Create a Gemini API key in Google AI Studio, then add it in:
# Vercel Project -> Settings -> Environment Variables
GEMINI_API_KEY=

# Optional. The app defaults to gemini-3.5-flash-lite when this is omitted.
GEMINI_MODEL=gemini-3.5-flash-lite
